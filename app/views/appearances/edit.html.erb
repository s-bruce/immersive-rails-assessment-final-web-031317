<h1>Edit an Appearance</h1>

<%= form_for @appearance do |f| %>
  <%= f.label :rating %>
  <%= f.number_field :rating, in: (1..5) %>
  <%= f.collection_select :episode_id, Episode.all, :id, :date %>
  <%= f.collection_select :guest_id, Guest.all, :id, :name %>
  <%= f.submit %>
<% end %>

<!-- I did not add a field for user/producer since the user/producer
should be the user who is logged in. I don't want the user to be able
to change the user/producer. -->